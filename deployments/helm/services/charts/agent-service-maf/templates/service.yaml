{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-service-maf.fullname" . }}
  labels:
    {{- include "agent-service-maf.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-service-maf.selectorLabels" . | nindent 4 }}
{{- end }}
