apiVersion: v2
name: artifact-service
description: A Helm sub-chart for AgentStudio's persistent artifact store (git-versioned per-store bare repos on the shared NFS PVC, MCP server endpoint at /mcp, REST viewer for GUI).
type: application
version: 1.0.0
appVersion: "0.1.0"
