# Dev/test dependencies for the ONTAP MCP server unit tests.
# Runtime deps (mcp, requests, observability-client-runtime) come from
# requirements.txt + the local observability package; see test_server.py.
pytest>=8.0.0
pytest-asyncio>=0.23.0
