// Strings consumed by `ConfigurationsPanel`. Kept in their own file so
// the eventual translator workflow can pick them up the same way as the
// sibling panels (toolsets, assigned-kb).
export const CONFIGURATIONS_PANEL_STRINGS = {
  RESOURCE_LABEL: "Configurations",
} as const;
