// Standalone re-export of sonner's toast function.
// Import this for triggering toasts — no component dependency needed.
// The Toaster component (toast.toaster.tsx) must be mounted once at app level for toasts to render.
export { toast } from "sonner"
